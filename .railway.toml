[build]
builder = "nixpacks"
buildCommand = "cd backend && chmod +x build.sh && ./build.sh"

[deploy]
startCommand = "cd backend && node dist/main.js"
healthcheckPath = "/health"
healthcheckTimeout = 100

[variables]
NODE_ENV = "production"
NPM_CONFIG_CACHE = "disabled" 